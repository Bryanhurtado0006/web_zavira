/*import { Client } from 'pg';

const client = new Client({
    host: 'localhost',
    port: 5433,
    user: 'postgres',
    password: 'root',
    database: 'Icfes'
});

client.connect();
export default client;
/*